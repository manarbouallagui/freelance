# Hendem - Backend (Flask + PostgreSQL)

## Prérequis
- Python 3.9+
- PostgreSQL (instance locale ou cloud)
- (Optionnel) virtualenv

## Installation
1. Clone le repo / crée un dossier:
```bash
mkdir hendem-backend
cd hendem-backend
# coller les fichiers app.py, models.py, requirements.txt, README.md
